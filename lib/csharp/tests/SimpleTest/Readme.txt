Sample: Root Echo and Child Echo

This is the simplest use case

A RootBot that forwards the activity it receives straight to a childbot using the connector and gets an 3 echo messages back.

The childbot sends an end conversation right after it sends the echo.